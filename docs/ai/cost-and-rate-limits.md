# Cost and rate limits

- Per-route in-memory rate limits
- Daily per-user `AI_DAILY_OPERATION_LIMIT`
- Output token caps and timeouts per operation
- One repair attempt max
- Token usage + approximate USD cost stored on `AiOperation` when available

Estimates are operational only and not shown to users.
